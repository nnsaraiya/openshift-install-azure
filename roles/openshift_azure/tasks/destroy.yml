---
- name: Run openshift-install destroy cluster
  ansible.builtin.command: "{{ bin_dir }}/openshift-install destroy cluster --dir {{ install_dir }} --log-level info"
  register: _destroy_cluster
  changed_when: true

- name: Show destroyer output
  ansible.builtin.debug:
    var: _destroy_cluster.stdout_lines
